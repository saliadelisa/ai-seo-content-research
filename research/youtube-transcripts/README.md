# YouTube Transcripts

Transcripts collected via Supadata API, organized by expert.

## How to Fetch

1. Add Supadata API key to `.env`: `SUPADATA_API_KEY=your_key`
2. Add video URLs to `scripts/fetch_transcripts.py`
3. Run: `python scripts/fetch_transcripts.py`